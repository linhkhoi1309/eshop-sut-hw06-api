# Stage S3: State Transitions Test Cases for API 1 (PUT /api/users/me)

## Test Cases

| ID | Title | Technique | Preconditions | Request Steps | Expected Status | Expected Body | Spec Justification |
|---|---|---|---|---|---|---|---|
| A1-S3-01 | Partial update on full profile (Completeness state) | State Transition | Logged in as user (id=2). Profile is fully populated: `name='Test User'`, `shipping_address='123 Le Loi...'`, `phone='0912345678'`. | 1. `PUT /api/users/me` with Body: `{"name": "Updated Name"}`<br>2. `GET /api/users/me` to verify. | 1. 200 OK<br>2. 200 OK | 2. `name` is "Updated Name". `shipping_address` and `phone` remain unchanged (not NULL). | "Người dùng đã đăng nhập có thể cập nhật: Họ Tên, Số điện thoại, Địa chỉ giao hàng mặc định." (Does not state omitted fields should be deleted; should preserve data). |
| A1-S3-02 | Restore full profile from partial state | State Transition | Logged in as user (id=2). Profile is partially populated (e.g., address and phone are NULL). | 1. `PUT /api/users/me` with Body: `{"name": "Full Name", "shipping_address": "New Address", "phone": "0987654321"}`<br>2. `GET /api/users/me` to verify. | 1. 200 OK<br>2. 200 OK | 2. `name`, `shipping_address`, and `phone` are all updated to the provided values. | "Người dùng đã đăng nhập có thể cập nhật: Họ Tên, Số điện thoại, Địa chỉ giao hàng mặc định." |
| A1-S3-03 | Privilege State: Role escalation attempt (user to admin) | Privilege Transition | Logged in as user (id=2, `role='user'`). | 1. `PUT /api/users/me` with Body: `{"name": "Hacker", "shipping_address": "Addr", "phone": "0912345678", "role": "admin"}`<br>2. `GET /api/users/me` to verify. | 1. 200 OK (or 403 Forbidden if strict rejection)<br>2. 200 OK | 1. Success message (or error if rejected).<br>2. `role` MUST remain "user". | "không thể tự thay đổi thuộc tính role" |
| A1-S3-04 | Privilege State: Role de-escalation attempt (admin to user) | Privilege Transition | Logged in as admin (`role='admin'`). | 1. `PUT /api/users/me` with Body: `{"name": "Admin User", "shipping_address": "Addr", "phone": "0912345678", "role": "user"}`<br>2. `GET /api/users/me` to verify. | 1. 200 OK (or 403 Forbidden if strict rejection)<br>2. 200 OK | 1. Success message (or error if rejected).<br>2. `role` MUST remain "admin". | "không thể tự thay đổi thuộc tính role" |
| A1-S3-05 | Stale token validation after DB role change | Privilege Transition / Auth | User has a valid JWT token issued when `role='user'`. DB state for role is modified directly to 'admin' (bypassing SUT). | 1. Send request to an admin-only endpoint (e.g., `GET /api/users`) using the original JWT token. | 1. 403 Forbidden | 1. Error message indicating insufficient permissions. | UNDETERMINED: Spec does not explicitly specify if role claims in JWT should be re-verified against DB on every request, but implicit security principle suggests old token shouldn't grant new privileges. |
| A1-S3-06 | Idempotent PUT request | Idempotency | Logged in as user (id=2). Profile has some initial state. | 1. `PUT /api/users/me` with Body: `{"name": "Stable", "shipping_address": "Stable Addr", "phone": "0911111111"}`<br>2. `PUT /api/users/me` with exact same Body.<br>3. `GET /api/users/me` to verify. | 1. 200 OK<br>2. 200 OK<br>3. 200 OK | 1. Success message<br>2. Same success message<br>3. Data matches the stable payload. | "Người dùng đã đăng nhập có thể cập nhật..." (Implies standard HTTP PUT idempotency behavior). |
